// Tailwind CSS v4 uses a dedicated PostCSS plugin (`@tailwindcss/postcss`).
// No tailwind.config.js is required in v4 — configuration is CSS-first.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
